services:
  cryptochat:
    build: .
    volumes:
      - cryptochat-data:/app/data
      - ./secrets/secret_key:/run/secret_key:ro
    environment:
      - SECRET_KEY_FILE=/run/secret_key
      - FLASK_ENV=production
    restart: unless-stopped

volumes:
  cryptochat-data:
